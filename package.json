{
  "name": "smarthome-lambda-keyword-controller",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda-based keyword trigger controller for SwitchBot-like devices",
  "scripts": {
    "test": "node --test",
    "get-device-info": "node --env-file=.env src/localGetDeviceInfo.js",
    "get-scene-info": "node --env-file=.env src/getSceneInfo.js",
    "execute-scene": "node --env-file=.env src/executeScene.js"
  }
}
